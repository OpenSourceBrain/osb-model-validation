# Framework for running automated tests on OSB using Travis-CI, see https://github.com/OpenSourceBrain/osb-model-validation

dist: bionic

addons:
  apt:
    packages:
       # Requirements for Brian & NEST
    - python-sympy
    - python-tornado
    #- cython
    - python-dev
    - python3-dev
        # Requirements for Octave
    - octave
        # Requirements for NetPyNE & libNeuroML
    - python-tk
    - python-lxml
    - python-tables
        # Requirements for Moose
    - libhdf5-dev
    - libgsl0-dev
    - g++


language: python

python:
  #- "2.7"
  #- "3.5"
  - "3.6"  # Required so nest has access to python 3.6 libraries outside of venv ...
  #- "3.7"
  #- "3.8"



env:
    #- OMV_ENGINE=Brian  # Fails on Py3 - Brian 1 not supported
    #- OMV_ENGINE=Brian2:2.2.2.1
    #- OMV_ENGINE=NEST:2.18.0
    #- OMV_ENGINE=NEST:2.20.0
    #- OMV_ENGINE=PyNEST
    #- OMV_ENGINE=Octave
    ##- OMV_ENGINE=genesis  #  No longer supported on OMV...
    ##- OMV_ENGINE=Moose  # Can this be built with py3?
    #- OMV_ENGINE=NEURON:7.8.2
    #- OMV_ENGINE=PyNEURON
    #- OMV_ENGINE=PyNN
    - OMV_ENGINE=PyNN_NEURON
    #- OMV_ENGINE=PyNN_Brian1
    #- OMV_ENGINE=PyNN_Nest
    #- OMV_ENGINE=PyNN_NeuroML
    #- OMV_ENGINE=NetPyNE
    #- OMV_ENGINE=PyLEMS
    #- OMV_ENGINE=PyLEMS_NeuroML2
    #- OMV_ENGINE=jLEMS
    #- OMV_ENGINE=jNeuroML
    #- OMV_ENGINE=jNeuroML_Brian
    #- OMV_ENGINE=jNeuroML_Brian2
    #- OMV_ENGINE=jNeuroML_Moose
    #- OMV_ENGINE=jNeuroML_NEURON
    #- OMV_ENGINE=jNeuroML_NetPyNE
    - OMV_ENGINE=jNeuroML_PyNN_NEURON
    #- OMV_ENGINE=jNeuroML_validate
    #- OMV_ENGINE=jNeuroML_validatev1
    #- OMV_ENGINE=Py_neuroConstruct
    #- OMV_ENGINE=pyNeuroML

    - OMV_ENGINE=DIRECT_ENGINE_INSTALL


install:
    - pip install numpy scipy matplotlib cython # Why is this needed? Scipy doesn't seem to get installed correctly on trusty with addons above...
    - python setup.py install

script:
    - omv list-engines -V
    - cd utilities/tests

    # Note --ignore-non-py3  --> doesn't install simulator or run tests or cause failure if simulator itself is not Python 3 compatible...
    - omv all -V --ignore-non-py3; export OMV_SUCCESS=$?; echo $OMV_SUCCESS

    - omv list-engines -V


    - python -c "from sysconfig import get_paths as gp; print(gp())"
    - env
    - echo "Finished all OMV tests on *.omt files which have been found"
    - if [[ ${OMV_ENGINE} == "DIRECT_ENGINE_INSTALL" ]]; then echo "Continuing..."; else exit $OMV_SUCCESS; fi


    - pip install neuron


    - omv list-engines -V

    - which nrniv
    - which python
    - env
